import { Dimensions, StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  image: {
    width: '100%',
    height: '100%',
  },
  touchableWrapper: {
    width: '100%',
    height: '100%',
  },
});

export default styles;
