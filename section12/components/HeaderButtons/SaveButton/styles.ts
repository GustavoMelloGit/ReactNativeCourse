import { StyleSheet } from 'react-native';
import theme from '../../../global/theme';

const styles = StyleSheet.create({
  button: {
    marginRight: theme.layout.headerSpacing,
  },
});

export default styles;
